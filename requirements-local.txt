# Ноутбук / WSL2, только CPU. Всё зафиксировано жёстко (§11.3).
# Установка:
#   pip install -r requirements-local.txt --index-url https://download.pytorch.org/whl/cpu \
#               --extra-index-url https://pypi.org/simple
torch==2.5.1
torchvision==0.20.1
numpy==2.1.3
pyyaml==6.0.2
matplotlib==3.9.2
pandas==2.2.3
scikit-learn==1.5.2
torchmetrics==1.5.2
jupyterlab==4.3.1

# Инструменты: на них уже ссылаются Makefile (test, lint) и pyproject ([tool.ruff]).
pytest==8.3.4
ruff==0.8.4

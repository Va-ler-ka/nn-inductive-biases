# Colab. torch и torchvision НЕ указаны сознательно (§11.3): берём те, что
# уже стоят в среде выполнения. Переустановка стоит перезапуска runtime
# и регулярных конфликтов с предустановленными пакетами.
pyyaml>=6.0
torchmetrics>=1.4
# torch_geometric ставится в M5 под фактическую версию torch:
#   pip install torch_geometric
